import React, { Component } from 'react'

class Todos extends Component {
    constructor(prosp) {
        super(prosp);
    }

    render(){
        return (
            <div>
                
            </div>
        );
    }
}

export default Todos;